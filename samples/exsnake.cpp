bool gameEnd() {}
void controlLoop() {}

int main() {
    /* start 'function' in here
     * declare global variables
     *  - count globals to correctly ORG
     *    for function declarations?
     */ 

    while (!gameEnd()) {
        controlLoop();
    }

    /* BAL gameEnd()
     * CMP R6 (return), #1
     * 
     * 
     * 
     */
}